import { v1 } from 'ask-smapi-model';
import SlotType = v1.skill.interactionModel.SlotType;
export declare enum SharedSlotType {
    FEEDBACK = "feedback",
    HEAD = "head",
    TAIL = "tail",
    ACTION = "action",
    TARGET = "target",
    CONJUNCTION = "conjunction",
    PREPOSITION = "preposition"
}
export interface ModelData {
    slotTypes: SlotType[];
    intentValues: IntentUtterances[];
}
export interface SlotValue {
    synonyms: string[];
    id: string;
}
export interface IntentUtterances {
    name: string;
    samples: string[];
}
export declare type ModelDataMap = {
    [locale: string]: ModelData;
};
//# sourceMappingURL=ModelTypes.d.ts.map