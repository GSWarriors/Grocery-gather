import { RequestHandler, Skill } from "ask-sdk-core";
/**
 * Wraps a user interface as a ASK Skill to be used in e2e scenario tests
 *
 * Deserialization requires some concrete objects information to rewire everything.. these are
 * passed as a simple IoC container.
 * The startingUserInterface parameter is only used on the first turn.  On subsequent turns the
 * UI is reconstituted from its serialized form in session attributes.
 *
 * @param startingUserInterface A clean UI to use as the staring point.
 * @param ioc
 */
export declare function wrapRequestHandlerAsSkill(requestHandler: RequestHandler): Skill;
//# sourceMappingURL=SkillWrapper.d.ts.map