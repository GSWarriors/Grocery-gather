import { IControl } from '../controls/interfaces/IControl';
/**
 * Creates a text diagram of the Control tree
 *
 * Notes:
 *  1. The handling control and its parents are highlighted in green
 *  2. The initiative control and its parents are highlighted in magenta
 *  3. If a control is a common parent to both the handling control and the initiative control, it is highlighted in yellow.
 * @param control
 * @param turnNumber
 * @param indent
 */
export declare function generateControlTreeTextDiagram(control: IControl, turnNumber: number, indent?: number): string;
//# sourceMappingURL=ControlTreeVisualization.d.ts.map