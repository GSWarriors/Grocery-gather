import { Intent } from "ask-sdk-model";
import { v1 } from 'ask-smapi-model';
import { BaseControlIntent } from './BaseControlIntent';
import SlotType = v1.skill.interactionModel.SlotType;
export interface OrdinalControlIntentSlots {
    feedback?: string;
    action?: string;
    target?: string;
    'AMAZON.Ordinal'?: string;
}
/**
 *
 *
 * empty slots are unpacked as 'undefined' to be more explicit about what is going on.
 * (this helps makes boolean tests in Controls more obvious and consistent)
 */
export declare function unpackOrdinalControlIntent(intent: Intent): OrdinalControlIntentSlots;
/**
 * Intent to capture an AMAZON.Ordinal, following the "ControlIntent" conventions
 */
export declare class OrdinalControlIntent extends BaseControlIntent {
    static of(slots: OrdinalControlIntentSlots): Intent;
    generateIntent(): v1.skill.interactionModel.Intent;
    generateSlots(): v1.skill.interactionModel.SlotDefinition[];
    generateSlotTypes(): SlotType[];
}
//# sourceMappingURL=OrdinalControlIntent.d.ts.map