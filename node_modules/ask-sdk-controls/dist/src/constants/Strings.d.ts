export declare namespace Strings {
    /**
     * Built-in slot types
     */
    enum SlotType {
        AmazonDate = "AMAZON.DATE",
        AmazonTime = "AMAZON.TIME",
        AmazonOrdinal = "AMAZON.Ordinal",
        AmazonNumber = "AMAZON.NUMBER"
    }
    /**
     * Built-in feedback slot values
     *
     */
    enum Feedback {
        Affirm = "affirm",
        Disaffirm = "disaffirm"
    }
    /**
     * Built-in action slot values
     */
    enum Action {
        Set = "set",
        Change = "change",
        Select = "select",
        None = "none"
    }
    /**
     * Built-in target slot values
     */
    enum Target {
        It = "it"
    }
}
//# sourceMappingURL=Strings.d.ts.map