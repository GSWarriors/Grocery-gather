import { ControlInteractionModelGenerator } from '../../interactionModelGeneration/ControlInteractionModelGenerator';
import { ModelData } from '../../interactionModelGeneration/ModelTypes';
/**
 * Optional interface for Controls that wish to customize Interaction Model.
 */
export interface InteractionModelContributor {
    /**
     * Slot Value IDs that must appear in the Interaction Model "Target" type to access functionality of the Control.
     *
     * Framework behavior:
     * * The Interaction Model generator will check that all these slot value IDs exist in the Target Type.
     */
    getTargetIds(): string[];
    /**
     * Updates the interaction model
     *
     * Usage:
     *
     * TODO: improve this jsDoc
     * All kinds of Controls are allowed to update InteractionModel through this function by using the adder functions provided in IMGenerator
     *
     * For Controls depend on controlIntent, it would be much easier to update the IM by using the util function
     * 'addControlIntent' provided in lib/interactionModelGeneration/ControlInteractionModelGenerator. As the controlIntent.generateIntent()
     * could provide the intent / slotType skeleton, and the sample utterances and slotType values will be provided by ModelData instance
     *
     * @param generator Interaction Model generator
     * @param locale The locale code E.G en-US, de-DE, ja-JP. REf: https://developer.amazon.com/en-US/docs/alexa/custom-skills/develop-skills-in-multiple-languages.html#h2-code-changes
     * @throws {LocaleNotSupportedError} This error will be throw if the support for input locale is not available
     */
    updateInteractionModel(generator: ControlInteractionModelGenerator, imData: ModelData): void;
}
/**
 * Type-guard for the InteractionModelContributor interface.
 *
 * @param arg Object to test
 * @returns true if the argument implements the InteractionModelContributor interface.
 */
export declare function implementsInteractionModelContributor(arg: any): arg is InteractionModelContributor;
//# sourceMappingURL=InteractionModelContributor.d.ts.map